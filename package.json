{
  "name": "ia-tools",
  "private": true,
  "description": "Centralized AI toolbox for development teams",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "check:slack-bridge-dist": "bash scripts/check-slack-bridge-dist.sh",
    "test:scaffold": "bash plugins/scaffold/scripts/smoke-test.sh",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "prepare": "command -v pre-commit >/dev/null && pre-commit install --install-hooks || echo 'pre-commit not installed; skipping hook setup'"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  },
  "packageManager": "pnpm@9.12.0"
}
