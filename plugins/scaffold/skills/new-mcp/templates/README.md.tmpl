# {{NAME}}

TODO: one-paragraph description.

## Tools

- `example_tool` — TODO: describe.

## Install

Installed as a Claude Code plugin. To build from source:

```bash
cd plugins/{{NAME}}
pnpm install
pnpm build
```

## Environment variables

TODO: list required + optional env vars. Example:

- `MY_TOKEN` (required) — API token for X
- `MY_URL` (optional, default `http://localhost:3800`) — base URL

## Development

```bash
pnpm dev         # watch-mode rebuild
pnpm typecheck   # tsc --noEmit
pnpm test        # vitest
```

## Architecture

TODO: explain transport, auth, state model.
