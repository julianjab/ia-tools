{
  "mcpServers": {
    "slack-bridge": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp-server.js"],
      "env": {
        "DAEMON_URL": "${DAEMON_URL:-http://localhost:3800}"
      }
    }
  }
}
