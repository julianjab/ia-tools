# Lo que el AgentDefinition del engine no tiene y Claude Code necesita.
# Sobrevive a cada `sync-agents.py`.
description: >-
  Paso `Refine` del pipeline de ia-flow para un issue TÉCNICO de un repo
  backend del board lahaus-ai-flow: produce el PRD verificado contra el código
  real. Lo despacha `/aiflow`; a mano, pedilo por nombre. Para refinar un
  issue de cualquier repo FUERA del pipeline, el agente es `issue-refiner`, no
  este.
color: cyan
maxTurns: 60
