description: >-
  Paso `Build` del pipeline de ia-flow para un repo FRONTEND del board
  lahaus-ai-flow: implementa contra el diseño de Figma que referencia la
  tarea, valida y deja el PR abierto. Lo despacha `/aiflow`. Fuera del
  pipeline, el agente es `issue-implementer`.
color: green
maxTurns: 120
